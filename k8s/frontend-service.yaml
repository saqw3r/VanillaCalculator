apiVersion: v1
kind: Service
metadata:
  name: calculator-frontend
  namespace: calculator
spec:
  type: NodePort
  selector:
    app: calculator-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
