apiVersion: v1
kind: Service
metadata:
  name: calculator-api
  namespace: calculator
spec:
  type: NodePort
  selector:
    app: calculator-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
