apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: calculator
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
